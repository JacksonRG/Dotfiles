git clean -n
	dry run of git clean. Shows what will be cleaned

	clean gets rid of untracked changes

git config --global core.excludesfile $HOME/.gitignore
	or wherever you want to put it. maybe ~/.config/git/exclude
	so you dont have to keep adding *.swp everywhere

git reset -- <path>
	resets to last change the file at path

git restore --staged <path>
	unstages all changes at <path>
	use . for <path> to unstage everything

git checkout <COMMIT#> <path/to/file>
