<prefix> $
	rename session

<prefix> ,
	Rename Window

<prefix> .
	Renumber Window

<prefix> =
	##AFTER COPYING SOMETHING
	list buffers, enter on one to print it

<prefix> w
	list panes in a tree to switch between

<prefix> m
	marks a pane.
	marked pane will be used by other commands.

:joinp
	alias for join-pane
	if no pane specified, swaps with marked pane

:swapp
	alias for swap-pane
	swaps current pane for specified one
	if none specified, swaps with marked pane

:breakp
	breaks current pane
	into its own window

